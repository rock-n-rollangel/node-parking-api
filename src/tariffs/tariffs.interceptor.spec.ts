import { TariffsTimeSliceInterceptor } from './tariffs.interceptor';

describe('TariffsInterceptor', () => {
  it('should be defined', () => {
    expect(new TariffsTimeSliceInterceptor()).toBeDefined();
  });
});
